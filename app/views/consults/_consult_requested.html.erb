<% if current_user.id == @consult.creator_id %>

<% elsif current_user.id = @consult.customer_id %>

<%= render 'cust_initial_confirm' %>
<%= render 'cust_initial_change' %>

<% elsif alpine_session? %>

<%= render 'alpine_initial_confirm' %>
<%= render 'alpine_initial_change' %>

<% end %>