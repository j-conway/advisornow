<% if @status != "Open" %>
  <p><%= link_to "View Open Tickets" %></p>
<% else %>
  <p><%= link_to "View Closed Tickets", {:status => "Closed"} %></p>
<% end %>
<table class="table table-hover" summary="Consults">
  <thead>
    <tr>
      <th>Subject</th>
      <th>Last Updated</th>
      <th>Data Scientist</th>
      <th>Primary Customer</th>
    </tr>
  </thead>
  <tbody>
    <%= render @consults %>
    
  </tbody>
</table> 
