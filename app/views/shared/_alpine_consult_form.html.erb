<div class="row">
<%= form_for [@company, @consult] do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="col-lg-6 col-md-6 col-sm-6">

  <%= f.label :subject, "Subject" %>
  <%= f.text_field :subject, :class => 'form-control' %>

  <%= f.label :description, "Description" %>
  <%= f.text_area :description, size: "25x5", :class => 'form-control' %>
  </div>
  <div class="col-lg-6 col-md-6 col-sm-6">

  <%= f.label :requested_length, 'Meeting length (hours)?' %>
  <%= f.select(:requested_length, @requested_lengths.map { |value| [ value, value ]}, {}, {:class => 'form-control'}) %>

  <%= f.label :requested_date_field, 'What day you want to meet?' %>
  <%= f.text_field :requested_date_field, :class => 'form-control date' %>

  <%= f.label :requested_time_field, 'What time (Pacific)?' %>
  <%= f.text_field :requested_time_field, :class => 'form-control timepicker', :id => 'timeInput' %>

  </div>
  </div>
  <div class="row">
    <div class="col-lg-6 col-md-6 col-sm-6">
  <h2><%= @company.name %> Customer</h2>
  <%= f.collection_select(:customer_id, @users.all, :id, :name, {}, {:class => 'form-control'}) %>
    </div>
    <div class="col-lg-6 col-md-6 col-sm-6">
  <h2>Alpine Data Scientist</h2>
  <%= f.collection_select(:datascientist_id, User.datascientists.all, :id, :name, {}, {:class => 'form-control'}) %>
  </div>
</div>

</br>
<div class="row">
  <div class="col-lg-2 col-md-2 col-centered">
<%= f.submit "Submit", class: "btn btn-large btn-primary" %>
</div>
</div>
</div>
<% end %>