<% provide(:title, @user.name) %>
  <% if @user.consults.any? %>
    <h1><%= @user.name + "'s" %> Consultations</h1>
    <table class="table table-hover" summary="Consults">
      <thead>
        <tr>
          <th>Subject</th>
          <th>Data Scientist</th>
        </tr>
      </thead>
      <tbody>
        <%= render @consults %>
      </tbody>
    </table>
    <%= will_paginate @consults %>
  <% end %>

